// TODO: add integration testing for rocket

// use rocket::{futures::FutureExt, local::blocking::Client};

// #[rocket::async_test]
// async fn create_account() {
//     let client = Client::tracked(super::launch().await).unwrap();
// }
